body { font-family: Arial, sans-serif; margin: 0; padding: 0; color: #333; }
.container { width: 80%; margin: 0 auto; }

/* Header */
header { border-bottom: 1px solid #ccc; padding: 10px 0; }
.nav-bar { display: flex; justify-content: space-between; align-items: center; }
nav a { margin: 0 15px; text-decoration: none; color: black; }

/* Hero Section */
.hero { display: flex; padding: 50px 0; border-bottom: 1px solid #ccc; }
.hero-content { flex: 1; padding-right: 20px; }
.hero-image { flex: 1; background: #eee; display: flex; align-items: center; justify-content: center; height: 200px; color: #999; }

/* Buttons */
button { padding: 10px 20px; border: 1px solid #ccc; cursor: pointer; border-radius: 4px; margin-right: 10px; }
.btn-yellow { background: #ffe4b5; }
.btn-blue { background: #d0e8f2; }
.btn-pink { background: #f8d7da; }

/* News Cards */
.news-grid { display: flex; gap: 20px; padding: 40px 0; }
.card { flex: 1; border: 1px solid #333; padding: 10px; }
.card-img { height: 120px; background: #f9f9f9; border-bottom: 1px solid #333; margin-bottom: 10px; display: flex; align-items: center; justify-content: center; }

/* Promo Block */
.promo { background: #d4edda; text-align: center; padding: 40px 0; margin-top: 20px; }

/* Footer */
footer { background: #fff3cd; padding: 30px 0 10px 0; margin-top: 50px; }
.footer-content { display: flex; justify-content: space-between; padding-bottom: 20px; }
.author { text-align: center; background: #ffe4b5; padding: 10px; font-size: 14px; }